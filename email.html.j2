<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Open CFPs and Sponsorships</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+jjXkk+Q2h455rYXK/7HAuoJl+0I4" crossorigin="anonymous"></script>
</head>

<body>
<div class="container">
    <h1>Global DevOpsDays Open CFPs and Sponsorships</h1>

   <p>Below is a monthly output of the open DevOpsDays CFPs and Sponsorships for your convience. Thank you for the interest in our events, and looking forward to engaging with you in our community.</p>
</div>

<div class="container">
   <h2>Open CFPs</h2>
   <table class="table">

   <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Call for Papers</th>
      <th scope="col">Registration Link</th>
    </tr>
   </thead>
   <tbody>
	 {% for event in cfp %}
        <tr>
	    <th scope="row">{{ event.count }}</th>
        {% if event.main_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href={{ event.main_link }}>{{ event.name }}</a></td>
        {% endif %}
        {% if event.cfp_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href={{ event.cfp_link }}>CFP</a></td>
        {% endif %}
        {% if event.reg_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href={{ event.reg_link }}>Registration</a></td>
        {% endif %}
    {% endfor %}
   </tbody>
  </table>
</div>

<div class="container">
   <h2>Open Sponsorships</h2>
   <table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Call for Papers</th>
      <th scope="col">Registration Link</th>
    </tr>
   </thead>
   <tbody>
    {% for event in sponsorship %}
    <tr>
	    <th scope="row">{{ event.count }}</th>
        {% if event.main_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href="https://devopsdays.org/events/{{ event.name }}/sponsor">{{ event.name }}</a></td>
        {% endif %}
        {% if event.cfp_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href={{ event.cfp_link }}>CFP</a></td>
        {% endif %}
        {% if event.reg_link == None %}
	    <td>None</td>
        {% else %}
	    <td><a href={{ event.reg_link }}>Registration</a></td>
        {% endif %}
    </tr>
	{% endfor %}
   </tbody>
  </table>
</div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js" integrity="sha384-oBqDVmMz9ATKxIep9tiCxS/Z9fNfEXiDAYTujMAeBAsjFuCZSmKbSSUnQlmh/jp3" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.min.js" integrity="sha384-cuYeSxntonz0PPNlHhBs68uyIAVpIIOZZ5JqeqvYYIcEL727kskC66kF92t6Xl2V" crossorigin="anonymous"></script>
</body>
</html>

